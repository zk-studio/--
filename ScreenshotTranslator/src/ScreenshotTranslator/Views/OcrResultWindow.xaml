<Window x:Class="ScreenshotTranslator.Views.OcrResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OCR 文字识别结果"
        Width="600" Height="450"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🔍 OCR 识别结果" FontSize="16" FontWeight="SemiBold"
                   Margin="0,0,0,12" Foreground="{StaticResource TextPrimaryBrush}" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 原图缩略 -->
            <Border Grid.Column="0" Margin="0,0,8,0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
                <Image x:Name="PreviewImage" Stretch="Uniform" Margin="4" />
            </Border>

            <!-- 识别文字 -->
            <TextBox Grid.Column="1" x:Name="OcrTextBox"
                     TextWrapping="Wrap" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     IsReadOnly="True"
                     FontSize="14" Padding="8"
                     Style="{StaticResource ModernTextBox}" />
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="📋 复制文字" Style="{StaticResource PrimaryButton}"
                    Margin="4,0" Click="CopyText_Click" />
            <Button Content="🌐 翻译" Style="{StaticResource PrimaryButton}"
                    Margin="4,0" Click="Translate_Click" />
        </StackPanel>
    </Grid>
</Window>
