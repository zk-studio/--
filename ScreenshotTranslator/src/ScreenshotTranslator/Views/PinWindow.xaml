<Window x:Class="ScreenshotTranslator.Views.PinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="钉图"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        SizeToContent="WidthAndHeight">

    <Border BorderBrush="#2196F3" BorderThickness="2" CornerRadius="2"
            Background="White">
        <Grid>
            <Image x:Name="PinnedImage" Stretch="Uniform" />
            
            <!-- 右键菜单 -->
            <Grid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="📋 复制" Click="Copy_Click" />
                    <MenuItem Header="💾 保存" Click="Save_Click" />
                    <Separator />
                    <MenuItem Header="🔍 OCR" Click="Ocr_Click" />
                    <MenuItem Header="🌐 翻译" Click="Translate_Click" />
                    <Separator />
                    <MenuItem Header="❌ 关闭" Click="Close_Click" />
                </ContextMenu>
            </Grid.ContextMenu>
        </Grid>
    </Border>
</Window>
