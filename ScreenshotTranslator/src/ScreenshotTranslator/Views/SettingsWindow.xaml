<Window x:Class="ScreenshotTranslator.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置"
        Width="550" Height="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 快捷键设置 -->
                <TextBlock Text="⌨️ 快捷键设置" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,8" />
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="区域截图" VerticalAlignment="Center" Margin="0,4" />
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="HotkeyRegion" Style="{StaticResource ModernTextBox}" Margin="0,4" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="全屏截图" VerticalAlignment="Center" Margin="0,4" />
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="HotkeyFullScreen" Style="{StaticResource ModernTextBox}" Margin="0,4" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="截图翻译" VerticalAlignment="Center" Margin="0,4" />
                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="HotkeyTranslate" Style="{StaticResource ModernTextBox}" Margin="0,4" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="OCR 识别" VerticalAlignment="Center" Margin="0,4" />
                    <TextBox Grid.Row="3" Grid.Column="1" x:Name="HotkeyOcr" Style="{StaticResource ModernTextBox}" Margin="0,4" />
                </Grid>

                <!-- 翻译设置 -->
                <TextBlock Text="🌐 翻译设置" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,8" />
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="源语言" VerticalAlignment="Center" Margin="0,4" />
                    <ComboBox Grid.Row="0" Grid.Column="1" x:Name="SourceLangCombo" Margin="0,4" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="目标语言" VerticalAlignment="Center" Margin="0,4" />
                    <ComboBox Grid.Row="1" Grid.Column="1" x:Name="TargetLangCombo" Margin="0,4" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="OCR 语言" VerticalAlignment="Center" Margin="0,4" />
                    <ComboBox Grid.Row="2" Grid.Column="1" x:Name="OcrLangCombo" Margin="0,4" />
                </Grid>

                <!-- 保存设置 -->
                <TextBlock Text="💾 保存设置" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,8" />
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="保存目录" VerticalAlignment="Center" Margin="0,4" />
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="SaveDirText" Style="{StaticResource ModernTextBox}" Margin="0,4" />
                    <Button Grid.Row="0" Grid.Column="2" Content="..." Width="30" Margin="4,4,0,4"
                            Click="BrowseSaveDir_Click" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="保存格式" VerticalAlignment="Center" Margin="0,4" />
                    <ComboBox Grid.Row="1" Grid.Column="1" x:Name="FormatCombo" Margin="0,4">
                        <ComboBoxItem Content="PNG" Tag="png" />
                        <ComboBoxItem Content="JPEG" Tag="jpg" />
                        <ComboBoxItem Content="BMP" Tag="bmp" />
                    </ComboBox>

                    <CheckBox Grid.Row="2" Grid.Column="1" x:Name="AutoSaveCheck"
                              Content="自动保存截图" Margin="0,4" />
                </Grid>

                <!-- 常规设置 -->
                <TextBlock Text="⚙️ 常规设置" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,8" />
                <StackPanel Margin="0,0,0,16">
                    <CheckBox x:Name="StartWithWindowsCheck" Content="开机自启" Margin="0,4" />
                    <CheckBox x:Name="MinimizeToTrayCheck" Content="最小化到托盘" Margin="0,4" />
                    <CheckBox x:Name="AutoCheckUpdateCheck" Content="自动检查更新" Margin="0,4" />
                </StackPanel>

                <!-- GitHub 仓库 -->
                <TextBlock Text="🔄 更新设置" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,8" />
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="GitHub 仓库" VerticalAlignment="Center" />
                    <TextBox Grid.Column="1" x:Name="GitHubRepoText"
                             Style="{StaticResource ModernTextBox}"
                             ToolTip="格式: owner/repo" />
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="恢复默认" Margin="4,0" Padding="16,8" Click="Reset_Click" />
            <Button Content="取消" Margin="4,0" Padding="16,8" Click="Cancel_Click" />
            <Button Content="保存" Style="{StaticResource PrimaryButton}" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
