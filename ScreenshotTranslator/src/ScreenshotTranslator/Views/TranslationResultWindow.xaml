<Window x:Class="ScreenshotTranslator.Views.TranslationResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="截图翻译结果"
        Width="800" Height="550"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🌐 截图翻译结果" FontSize="16" FontWeight="SemiBold"
                   Margin="0,0,0,12" Foreground="{StaticResource TextPrimaryBrush}" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 原图 + 翻译叠加 -->
            <Border Grid.Column="0" Margin="0,0,8,0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Image x:Name="OriginalImage" Stretch="Uniform" Margin="4" />
                    <Canvas x:Name="TranslationOverlay" />
                </Grid>
            </Border>

            <!-- 文字对比 -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="原文" FontWeight="SemiBold" Margin="0,0,0,4" />
                <TextBox Grid.Row="1" x:Name="OriginalTextBox"
                         TextWrapping="Wrap" IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="13" Padding="8"
                         Style="{StaticResource ModernTextBox}"
                         Margin="0,0,0,8" />

                <TextBlock Grid.Row="2" Text="翻译" FontWeight="SemiBold" Margin="0,0,0,4" />
                <TextBox Grid.Row="3" x:Name="TranslatedTextBox"
                         TextWrapping="Wrap" IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="13" Padding="8"
                         Style="{StaticResource ModernTextBox}" />
            </Grid>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="📋 复制原文" Style="{StaticResource PrimaryButton}"
                    Margin="4,0" Click="CopyOriginal_Click" />
            <Button Content="📋 复制翻译" Style="{StaticResource PrimaryButton}"
                    Margin="4,0" Click="CopyTranslation_Click" />
            <Button Content="📋 复制全部" Style="{StaticResource PrimaryButton}"
                    Margin="4,0" Click="CopyAll_Click" />
        </StackPanel>
    </Grid>
</Window>
